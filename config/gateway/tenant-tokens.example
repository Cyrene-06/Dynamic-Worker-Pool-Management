# 示例：租户令牌表。
#
# 格式（一行一个）：token=tenant[:subject]
#   - token   ：至少 32 字符，随机生成。短令牌可被暴力枚举，而它保护的
#               是一个租户的全部沙箱，因此过短会**拒绝启动**（不是警告）。
#   - tenant  ：授权的基本单位。所有配额、可见性、复用规则都按它判定。
#   - subject ：可选，仅用于审计。
#
# ⚠️ 千万不要把真实令牌提交进仓库。真实文件应当通过 Secret 挂载，
# 由运维在部署时创建。本文件只是格式示例。
#
# 生成方式（任选）：
#   openssl rand -hex 32
#   powershell -c "[guid]::NewGuid().ToString('N') + [guid]::NewGuid().ToString('N')"
#
# 轮换：本进程只在启动时读取该文件，因此轮换令牌需要滚动重启 Pod。
# 这是一个刻意的取舍 —— 热加载会引入"配置改了但行为没变"这类很难排查的
# 问题，而滚动重启在双副本下是秒级的、且不影响已有沙箱。

# tenant-a 的两个使用者
1111111111111111111111111111111111111111111111111111111111111111=tenant-a:user:alice
2222222222222222222222222222222222222222222222222222222222222222=tenant-a:service:agent-runner

# tenant-b
3333333333333333333333333333333333333333333333333333333333333333=tenant-b
