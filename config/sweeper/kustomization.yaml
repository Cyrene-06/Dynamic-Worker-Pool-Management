apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 对账 CronJob。同样依赖 config/rbac 里的 ServiceAccount 与 ClusterRole，
# 部署顺序：先 `kubectl apply -k config/rbac`，再 apply 本目录。
resources:
  - cronjob.yaml
